apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cipms.fullname" . }}-test-connection"
  labels:
    {{- include "cipms.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cipms.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
